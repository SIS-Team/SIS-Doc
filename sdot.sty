% sdot.sty 
% Buchberger Florian
%
% Based on graphvizzz.sty by Ives van der Flaas
% Based on graphviz.sty by Mark Aufflick 2006-03-25
% Based on graphviz.tex by Derek Rayside 2003

% add the following lines to your preamble:

% \usepackage{tikz}
% \usetikzlibrary{snakes,arrows,shapes}
% \usepackage{amsmath}
% \usepackage{sdot}

\ProvidesPackage{sdot}

% parameters to \fdot:
% 1 - parameters for tikzpicture (optional; default value is "scale=1")
% 2 - filename of dot file (without extention)

\newcommand{\fdot}[2][scale=1]{ 
	\immediate\write18{dot2tex --figonly -t raw --autosize --graphstyle="#1" #2.dot > #2.tex}
	\IfFileExists{#2.tex}{
		% the tex exists: include it 
		\input{#2.tex}
	} {
		% an error
		Datei nicht verfügbar.
	} 
}